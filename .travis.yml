language: dart
rvm:
  stable

cache:
  pub-get

os:
 linux

addons:
  apt:
    packages:
      - lcov
      - lynx

after_success:
  - pub run test_coverage
  - genhtml -o coverage coverage/lcov.info 
  - lynx -dump coverage/index.html
